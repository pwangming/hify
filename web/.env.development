# 开发环境变量（无密钥，入库）

# 接口基础路径：相对路径，开发走 Vite proxy（生产由 nginx 反代），两端一致
VITE_API_BASE_URL=/api/v1
# axios 超时（毫秒）
VITE_API_TIMEOUT=30000

# 本地后端代理目标（非 VITE_ 前缀：仅供 vite.config.ts 的 proxy 使用，不注入前端产物）
SERVER_PROXY_TARGET=http://localhost:8080
